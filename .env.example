# ---------- App runtime ----------
NODE_ENV=development
PORT=3000

# ---------- Database ----------
DATABASE_URL=postgresql://user:password@localhost:5432/oneto?schema=public

# ---------- Auth ----------
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET=generate-with-openssl-rand-base64-32
JWT_EXPIRES_IN=15m

# ---------- SMS (OTP delivery) ----------
# For dev, leave blank — FakeSmsProvider logs to console
TERMII_API_KEY=
TERMII_SENDER_ID=oneto

# ---------- Payments (Korapay) ----------
KORAPAY_PUBLIC_KEY=pk_test_xxx
KORAPAY_SECRET_KEY=sk_test_xxx
KORAPAY_WEBHOOK_SECRET=whsec_xxx