# Admin web API base URL
#
# Local dev with Vite proxy:
# - Leave this empty and run `pnpm --filter admin-web dev`
# - Requests go to /auth and /admin and are proxied to localhost:3000
#
# Deployed admin-web:
# - Set this to your backend URL, e.g. https://api.getoneto.com
VITE_API_BASE_URL=
